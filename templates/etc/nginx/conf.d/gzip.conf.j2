# {{ ansible_managed }}

gzip on;
gzip_comp_level {{ nginx_gzip_level }};
gzip_http_version 1.0;
gzip_proxied any;
gzip_disable "MSIE [1-6]\.";
gzip_types {%for _type in nginx_gzip_types %} {{ _type }} {%endfor%};
